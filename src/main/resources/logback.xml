<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
            </Pattern>
        </layout>
    </appender>

    <logger name="org.springframework.web" level="INFO">
        <appender-ref ref="STDOUT" />
    </logger>

    <!--<logger name="app.controller.MainController" level="debug">-->
        <!--<appender-ref ref="STDOUT" />-->
    <!--</logger>-->

    <!--    #Shows methods marked as @Transactional -->
    <logger name="log4j.logger.org.springframework.transaction" level="DEBUG">
        <appender-ref ref="STDOUT" />
    </logger>
    <!-- # FULL transaction trace -->
    <logger name="log4j.logger.org.springframework.orm.jpa.JpaTransactionManager"
            level="DEBUG">
        <appender-ref ref="STDOUT" />
    </logger>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>



</configuration>
